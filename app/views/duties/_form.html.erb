
<%= form_with(model: duty, local: true, class: 'form-horizontal') do |form| %>
  <% if duty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(duty.errors.count, "error") %> prohibited this duty from being saved:</h2>

      <ul>
      <% duty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label class="col-sm-2 control-label">Título:</label>
    <div class="col-sm-10">
      <%= form.text_field :title, class: 'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <label class="col-sm-2 control-label">Adicionar em:</label>
    <div class="col-sm-10">
     <%= select_tag 'activity', options_from_collection_for_select(@options_for_select_parent, 'id', 'description') %>
    </div>
  </div>

<div class="form-group">
    <label class="col-sm-2 control-label">Arquivo:</label>
    <label class="custom-file-upload"> <!-- /custom.css -->
    <%= form.file_field :file %>     
   </label>
  </div>



<div class="hr-line-dashed"></div>
  <div class="form-group">
    <div class="col-sm-4 col-sm-offset-2" style="margin-left: 90%;">

    <a href="/duties">
        <button class= "btn btn-danger" type="button">
          <i class="fa fa-times">
          </i>
          </button>
        </a>
        <button class= "btn btn-primary" type="submit">
          <i class="fa fa-check">
          </i>
          </button>

    </div>
  </div>

<% end %>

