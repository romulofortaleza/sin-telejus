<%= form_with(model: component_court, local: true) do |form| %>
  <% if component_court.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(component_court.errors.count, "error") %> prohibited this component_court from being saved:</h2>

      <ul>
      <% component_court.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="field">
    <%= form.label :organ %>
    <%= form.text_field :organ %>
  </div>

  <div class="field">
    <%= form.label :last_update %>
    <%= form.date_select :last_update %>
  </div>

  <div class="field">
    <%= form.label :public_defender %>
    <%= form.text_field :public_defender %>
  </div>

  <div class="field">
    <%= form.label :composition %>
    <%= form.text_area :composition %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
